{
  "name": "claude-for-commerce-examples",
  "private": true,
  "workspaces": ["vendor/web-shared", "storefront/web", "merchant/web"],
  "scripts": { "build": "npm run build --workspaces --if-present" }
}
